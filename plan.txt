1. **Create `pack.mcmeta`:** Set `pack_format: 101`, using `"supported_formats": {"min_inclusive": 101, "max_inclusive": 101}` or similar `min_format`/`max_format` as required.
2. **Create `data/minecraft/dimension_type/overworld.json`:** Set `min_y: -64`, `height: 2064` (up to Y=1999). Include `"monster_spawn_light_level": {"type": "minecraft:uniform", "min_inclusive": 0, "max_inclusive": 7}`.
3. **Create `data/minecraft/worldgen/noise_settings/overworld.json`:** Set `"noise": {"min_y": -64, "height": 2064, "size_horizontal": 1, "size_vertical": 2}`. Update `preliminary_surface_level` and `spawn_target` and ensure everything matches format 101 requirements. Ensure Density Functions are extracted! We need modularity for surface rules.
4. **Create Density Function Files:** The task requires extracting the Density Functions to standalone files under `data/minecraft/worldgen/density_function/overworld/`. In standard vanilla overworld JSON, there are strings referring to density functions like `minecraft:overworld/continents`. We must provide these if we're customizing Y-levels in them or if it's generally required. We should use standard 1.21 files.
